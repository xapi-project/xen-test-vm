dist: trusty
language: c
sudo: required
install: 
    - curl -O https://raw.githubusercontent.com/lindig/xen-test-vm/master/setup.sh 
    - curl https://raw.githubusercontent.com/lindig/xen-test-vm/master/travis.sh | bash -ex 
script:
    - bash -c 'eval $(opam config env) && make'
os:
    - linux
