dist: trusty
language: c
sudo: required
install: 
    - curl -O https://raw.githubusercontent.com/lindig/xen-test-vm/master/setup.sh 
    - curl -O https://raw.githubusercontent.com/lindig/xen-test-vm/master/travis.sh 
script:
    - bash travis.sh
os:
    - linux
